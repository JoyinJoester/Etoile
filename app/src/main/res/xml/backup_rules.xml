<?xml version="1.0" encoding="utf-8"?>
<!--
   Defense in depth for Android 11 and older. AndroidManifest.xml disables
   backup globally, while these exclusions ensure that an OEM manifest merge
   or future configuration change still cannot copy device-bound Steam data.
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
