[package]
name = "etoile_code_reader"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
jni = "0.21.1"

[profile.release]
lto = true
codegen-units = 1
strip = true

